{
  "name": "girlfriend-gift-website",
  "version": "1.0.0",
  "description": "Unified build script for automated deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
